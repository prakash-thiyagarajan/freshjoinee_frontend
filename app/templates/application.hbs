<div class="container">
  <h2 class='page-header'>
    {{#if session.isAuthenticated}}
      <button {{action "logout"}} class="btn btn-default pull-right">Log out</button>

    {{/if}}
  </h2>
</div>
<div>
  {{outlet}}
</div>
